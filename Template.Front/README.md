# Template.Front

Angular remote (Native Federation) — port `4202`, exposes `./Routes`.

- `npm start` — UI-only (MSW)
- `npm run start:api` — game-full DevGateway

Routes: `/` items demo, `/sheet` player workspace demo (see the repository README).
